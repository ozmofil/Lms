<!-- ===========================
     SCORM TAB
=========================== -->
<div class="tab-pane fade" id="scorm-tab" role="tabpanel" aria-labelledby="scorm-tab-link">

    <h5 class="mt-3">SCORM İçerik</h5>

    <!-- === SCORM BAŞLAT === -->
    <div class="mb-3 d-flex gap-2 align-items-center">
        <button
            id="load-scorm-btn"
            class="btn btn-sm btn-primary"
            data-course-id="course-123">
            Eğitimi Başlat
        </button>

        <small class="text-muted">
            SCORM eğitim S3 üzerinden yüklenecektir
        </small>
    </div>

    <!-- === SCORM IFRAME === -->
    <div class="border rounded mb-4" style="height:520px;">
        <iframe
            id="scorm-frame"
            style="width:100%; height:100%; border:none;"
            allowfullscreen>
        </iframe>
    </div>

    <hr />

    <!-- ===========================
         SCORM AKSİYON
    =========================== -->
    <h5>SCORM Aksiyon</h5>

    <div class="row mb-3">
        <div class="col-md-3">
            <label>Kullanıcı</label>
            <input
                id="scorm-userId"
                class="form-control"
                value="demo-user" />
        </div>

        <div class="col-md-3">
            <label>ContentId</label>
            <input
                id="scorm-contentId"
                class="form-control"
                value="course-123"
                readonly />
        </div>

        <div class="col-md-6">
            <label>Context</label>
            <textarea
                id="scorm-context"
                class="form-control"
                rows="1"></textarea>
        </div>
    </div>

    <!-- ===========================
         JSON PREVIEW
    =========================== -->
    <div class="mb-3">
        <label class="fw-bold">JSON Preview</label>
        <pre
            id="scorm-json-preview"
            class="bg-light p-3 border rounded"
            style="min-height:120px;">
{}
        </pre>
    </div>

    <!-- ===========================
         LOG TABLOSU
    =========================== -->
    <h6>Loglar</h6>

    <div class="table-responsive">
        <table class="table table-sm table-bordered">
            <thead class="table-light">
                <tr>
                    <th style="width:120px;">Zaman</th>
                    <th>Statement JSON</th>
                </tr>
            </thead>
            <tbody id="scorm-log-body">
                <!-- JS ile doldurulur -->
            </tbody>
        </table>
    </div>

</div>
